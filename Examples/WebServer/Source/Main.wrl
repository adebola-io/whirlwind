module Main;

use Core.Http.Server;

/// This is the entry to the program.
public function Main() {
    app := new Server();

    app.Get("/hello", fn (req, res) res.Status(200).Json("Hello, world!"));

    app.Listen(8080);
}

