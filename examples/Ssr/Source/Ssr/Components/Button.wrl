module Button;

use Core.Io.Fmt;
use Super.Component;

model Button implements Component + Display {
    var children: Array<Button>;
    public function [Display.ToStr](): String {
        template := Fmt(`<button>%</button>`);
        return template.Add(Array.ToStr(&this.children)).Finish();
    }
}