module TypeTest;

use Core.Primitives.Ops.Addition;
use Core.Collections.{Tuple, Quadruple};


/// Returns the value itself.
public function Identity<T>(value: T): T {
    value
}

// firstChar := fn(str: String) str.Chars().Next();
function FirstCharacter(str: String): Maybe<&String> {
    return str.Chars().Next();
}

function AFunction() {
    
}
