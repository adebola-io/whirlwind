module Io;

use Super.Primitives.String;

/// Creates a new format object.
public function Format(template: String): FormatObject {
    new FormatObject(template)
}

public model FormatObject {
    new(template: String) {
        
    }
}

public function Print(message: String) {
    
}