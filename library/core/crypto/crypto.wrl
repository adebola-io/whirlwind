module crypto;

use core.prelude.todo;
use core.prelude.String;

public model JwtEncryptor {
    /// Verify 
    public static function decode(name: String, secret: String) -> String {
        todo()
    }
}