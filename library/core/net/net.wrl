module net;

public use http;
use core.prelude.Display;
use core.prelude.String;
use core.prelude.todo;


public model Url implements Display {
    public function [Display.toStr]() -> String {
        todo()
    }
}