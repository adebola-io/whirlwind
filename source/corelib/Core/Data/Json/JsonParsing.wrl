module jsonParsing;

use super.Serializeable;
use super.RawJsonVal;
use core.prelude.Outcome;
use core.prelude.todo;

public model JsonParseError {

}

/// Converts an object that implements `Serializeable` into a JSON value.
public function stringify
    <T implements Serializeable>
    (value: T)
    : Outcome<RawJsonVal, JsonParseError> 
{
    return todo();
}