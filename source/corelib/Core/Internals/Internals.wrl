module internals;

use core.primitives.Bool;
use core.prelude.Display;

public use never.never;
public use builtin;
public use injunction.{Injunction, invoke};



public model StackTrace implements Display {
    // todo.    
}