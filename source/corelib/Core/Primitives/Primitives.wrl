module primitives;

public use string.{String, Chars};
public use array.Array;
public use bool.Bool;
public use numeric;
public use ops;
public use numeric.{
    UInt8,
    UInt,
    UInt16,
    UInt32,
    UInt64,
    SignedInt,
    UnsignedInt,
    Int,
    Float,
    Number,
}; 